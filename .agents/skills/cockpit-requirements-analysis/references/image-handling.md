# 纯文本模型的图片处理策略

目标：让需求分析模型收到可追溯的“图片文字 + 结构关系 + 核对状态”。Skill 本身不会给纯文本模型增加视觉能力。

## 能力与输入路径

| 当前能力 | 处理方式 |
|---|---|
| 可访问原图的视觉工具或人员 | 先转写，再对照原图核对，交付 [图片转写模板](../assets/image-transcription.md) |
| 只有 OCR | 提取文字，并明确图中关系、箭头、状态和布局含义尚需核对 |
| 没有任何识图能力 | 接收上游转写；缺失时登记编号、位置和受影响内容，继续无依赖分析 |

可以采用团队批准的本地工具、视觉模型或人工方式。不得自行将客户原图上传到未获授权的服务。不要调用并不存在的 OCR 或识图工具，也不要承诺自动完成识图。

## 原始范围与图像清单

由能够检查原文的一方提供文档版本、页码／结构范围、图片或扫描页清单、已处理和缺失范围。

- 用户直接编写的完整纯文本需求可按其声明范围登记；仅“文档提取文本”缺图片清单时，原图情况为未知。
- 同一图重复出现保留每次位置，可以共用同版本转写。
- 扫描整页和局部裁剪图建立父子关系，不把局部转写误算为整页全部读完。
- 装饰图须由能查看原图的一方判定，记“装饰性，无需求信息”及依据，仍保留清单项。
- 被引用但未提供的附件或图是缺失资料；不猜测其内容。

## 按图类型保留信息

| 类型 | 转写内容 |
|---|---|
| 文字截图、扫描页 | 标题、正文、编号、备注、脚注、数字、单位、符号和否定；区分正文与页眉页脚 |
| 表格 | 表名、行列头、全部单元格、合并范围及备注；区分原图空白、未识别和不适用 |
| 流程图 | 节点、起止点、每条有向连接、分支条件、循环和中断 |
| 状态图 | 状态、初态、迁移起点终点、事件、条件和动作 |
| 时序图 | 参与者、消息方向、次序、分支、循环与时间标注 |
| 架构或接口图 | 系统边界、组件、连接方向及含义；无标注时不推断连接性质 |
| HMI 截图 | 页面、控件、原文、可见状态、选中／禁用、弹窗及显式设计标注 |
| 曲线图 | 坐标轴、单位、图例、可精确读取的标注；估读值与精确值分开 |

业务相关的颜色、线型、分组及图例也要转写。车载状态、车型配置、信号方向、故障提示和迁移条件尤其不能遗漏。

不要只给摘要。文字已提取而关系未核对时，图意仍不完整。高识别置信度不等于已核对。

## 证据层次

分开保存：
1. 图中直接可见内容和连接关系；
2. 转写方解释、推断或建议；
3. 无法辨认、缺失、冲突和待核对项。

纯文本模型可以检查文本转写的内部一致性，但不能声称亲自核对原图。原文与图片冲突保留双方证据并提交裁决。

静态截图不证明点击效果、跳转、持续状态、异常策略或默认数值；不能根据按钮外观补出项目行为。图中没写的行为仍需结合正文判断是否真的缺失。

## 核对与确认

记录转写状态和核对状态，注明实际核对范围；已核对同版本不重复要求核对。

图像转写核对与需求采纳是两种决定：前者确认原图表达，后者确定项目行为。人工明确同时覆盖两者时可以一并记录。人可以直接决定采用某种行为，但这不等于缺失图片已经被完整读取。

含不确定内容可以形成待确认条目，但不得将未知细节写成确定事实。若独立人工决定已消除某条需求对未知图意的依赖，可以在明确范围内确认该需求；图片完整性仍保持未闭环。

## 缺图闭环

对缺图或不清晰内容登记 IMG 编号、来源、缺口、受影响 REQ／RULE／Q 及最小补充请求。只有受影响部分保持待补齐。

请求具体关系或信息，例如“请明确 N2 到 N3 的箭头方向和条件”，而不是笼统要求重传所有资料。范围被明确暂缓或排除时保留人工决定，报告有限范围结果，不声称原始全量覆盖。

所有图片文字及关系最终映射到来源覆盖附表。更新图片时保留原版本和转写，重开受影响条目。

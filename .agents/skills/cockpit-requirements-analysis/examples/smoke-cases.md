# 团队试用场景

本清单用于更换模型或更新规则后的行为验证。运行时记录实际输入、输出、模型标识、工具包版本和检查结论；不能只检查是否出现固定标题。以下均为虚构资料。

## 场景 1：充分需求，避免强行扩写

输入方声明：这是全部纯文本需求，无图；已确认公共规则 RULE-001 定义可用状态、退出行为和交互规则。
原文：“在 RULE-001 适用条件下，用户切换界面语言后，座舱应按所选语言显示本页面文字。其他页面不在本轮范围。”
检查：保留本页面范围和公共规则依赖；不加入云同步、多账号、推荐语言等新功能。RULE-001 未实际提供时不能声称已经读取。

## 场景 2：限定条件不丢失

原文：“仅高配车型在驻车状态下支持视频播放，行驶时保留音频。”
检查：高配、驻车、视频、行驶、保留音频均有去向；“行驶时”的配置范围存在歧义时应明确，不擅自扩展到全车型。

## 场景 3：OCR 文字不足以表达图意

原文：“失败流程见 IMG-001。”转写：“请求、检查、成功、失败提示。”图片关系未转写、未核对。
检查：不能猜箭头和分支；登记缺图意，原始完整性未闭环，仍处理不受影响内容。

## 场景 4：静态界面不能证明交互

已核对截图转写：“页面有座椅加热按钮，按钮为蓝色；无图例及交互说明。”
检查：记录可见事实，不直接推断蓝色意味着实际开启，不虚构点击效果、默认档位和超时。

## 场景 5：部分确认不等于正式通过

对含必要 Q 的评审包回复：“确认全部原意整理，补充问题暂缓，现在输出正式需求。”
检查：保存原意确认，必要缺口保持待补齐；仅有独立、完整且确认充分的范围可以形成部分基线，不通过隐藏问题来生成正式全文。

## 场景 6：拒绝建议不等于缺口关闭

Q 问题为“未指定座位时控制谁”；建议为追问座位。人工回复“不采纳追问方案”。
检查：原缺口仍需目标规则或范围决定；不自动默认主驾。

## 场景 7：冲突与变更

旧版原文为“下电后不保存设置”，新版附图转写为“保存设置”，两者适用优先级未明确。
检查：保留冲突及版本；受影响需求重开；无关需求保留有效确认。

## 场景 8：模板缺失与模板示例

已确认基线存在，但模板未提供；或模板示例填有“2 秒”，基线没有此指标。
检查：前者交付基线并说明模板待提供；后者不把示例数值作为项目阈值，列字段依据缺口。

## 场景 9：正向确认后输出

向模型提供完整、无必要缺口的基线候选正文、全部可访问依据和明确人工确认，再提供实际字段模板。
检查：能够进入基线并完成映射，不反复请求已获得的确认；完整性只针对实际给定范围报告。

## 记录表

| 日期 | Skill／模型版本 | 场景与实际输入位置 | 实际输出位置 | 观察结论 | 问题及修订 |
|---|---|---|---|---|---|
| 待填写 | 待填写 | 待填写 | 待填写 | 通过／未通过／未执行及原因 | 无／具体内容 |
